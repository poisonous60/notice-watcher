# dev-only — N100 배포엔 안 깔림. dev박스에서:
#   pip install -r requirements-dashboard.txt
# 그 뒤 `python scripts/dashboard.py` 로 띄움 (127.0.0.1:8765).
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
