TASK: register a notice-watcher config

Read AGENTS.md FIRST (in your cwd). Then:
- digest.json, slug.txt, url.txt
- failure_packet.json if present (previous api_loop_once failed; evidence only)
- examples/manifest.json (pick 1-2 relevant)

Target:
  slug = {{ slug }}
  url  = {{ url }}

HARD RULES (match AGENTS.md):

- READ ONLY files in your cwd (this tmpdir). NO paths outside.
- WRITE ONLY to ./candidate.json.
- Max 2 validate cycles.
- Final output = single JSON object. No prose.
- Aim for ≤4 tool calls total (read / write / validate / optional retry).
- No `headless: false` in config.

YOUR JOB: produce ./candidate.json that passes
    ./run_validator.sh ./candidate.json     # Linux
    .\run_validator.bat ./candidate.json    # Windows
…and emit the final JSON.

Begin.
