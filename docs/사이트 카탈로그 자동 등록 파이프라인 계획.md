# 사이트 카탈로그 자동 등록 파이프라인 계획 (rev5, 2026-05-20)

CONTEXT.md 의 **catalog** + **catalog batch run** 어휘. grill-with-docs 세션 11 결정으로 rev4 → rev5 재설계.

## 1. 동기 + 의도

**메인 목표는 `register.py` / probe / 휴리스틱 / prompt 자동화 품질 향상**. 그걸 측정·진단하려면 다양한 사이트 surface 가 N100 (실제 봇 env) 에서 측정되어야 함.

사용자가 Discord 에서 `/preview URL` 매번 치는 걸 자동화 = catalog batch run. 카탈로그 entry 마다 jobs 테이블에 enqueue → bot worker 가 일반 path (`/preview` 와 똑같이) 로 처리. dev 박스에서 register.py 직접 돌리면 IP/네트워크/fingerprint 가 N100 과 달라 진단 가치 X — rev3 의 실수.

rev5 추가: catalog 를 *파일 1개에서 multi-file 로 분리*. 사용자가 한 번에 추가한 chunk 마다 새 파일 → git 편집 가벼움. 분류 의도 X — 의미적으론 1 monotonic pool.

## 2. 모델 (rev5)

- **catalog** = name+url 짝 모음 1개. 파일 1개 (`configs/candidates/<name>.yaml`, 파일명 stem = catalog 이름).
- **catalog 의 의미** = 사용자가 한 chunk 로 박은 entry 들의 *편집 편의 partition*. 분류·코호트 X.
- **합집합 = monotonic pool** — 사용자가 아직 안 해본 사이트 후보 전체.
- **cross-catalog dedup 강제** — 같은 url 두 파일에 박이면 driver load 시 fail.
- **이름 가벼움** — `2026-05-20.yaml`, `chunk-2.yaml`, `legacy-2026-05-19.yaml` 식. regex `^[a-z0-9][a-z0-9_-]{0,63}$`.

## 3. 목표 / 비목표

**메인 목표**
- `register.py` / probe / 휴리스틱 / prompt 자동화 품질 향상.
- 외부 조사 사이트 목록을 **machine-readable 카탈로그** (name + url 만) 로 변환.
- 카탈로그를 입력으로 받아 **N100 의 bot worker 큐** 에 일괄 enqueue 하는 driver.
- dashboard 에 **카탈로그별 진행률 + status × fail_kind/fail_subkind 분포** 화면.
- 카탈로그 종류 무관 — 게시판 형태면 모두 admit. *게시판 아닌 페이지도 의도적으로 일부 섞음* (gate_reject path 측정).

**비목표**
- Cloudflare/CAPTCHA 우회 (정책 — 차단되면 거부로 카운트, `docs/크롤링 지침.md` §2).
- dev 박스에서 register.py 직접 돌리기.
- **catalog 분류 / 코호트화** — 사용자 분류 의도 없음 (grill-with-docs 세션 11 결정).
- **dashboard 에서 batch run 트리거 버튼** — Claude/CLI 가 트리거. dashboard 는 view + entry add 만.

## 4. 카탈로그 schema (rev5 — multi-file)

위치: `configs/candidates/<name>.yaml`. 각 파일 git 추적. 같은 schema 2.

```yaml
version: 1
schema: 2
entries:
  - name: 마비노기 (PC) 공지
    url: https://mabinogi.nexon.com/page/news/notice_list.asp
  - name: 메이플스토리 (모바일) 공지
    url: https://m.maplestory.nexon.com/board/7/
```

필드 2개:
- `name` (필수, 문자열, 1~200자) — 사람 읽는 라벨.
- `url` (필수, http(s), ≤1000자).

**dedup**:
- 같은 url 한 catalog 안에 두 번 X (이미 rev4).
- 같은 url **cross-catalog** 두 파일에 X (rev5 신규). driver load 시 다 파일 합치고 검증 — 위반 시 stderr 에 위반 url + 두 파일명 출력 + exit 2.

**이름 자동**:
- dashboard "+ 새 catalog" 시 사용자가 이름 비우면 `auto-YYYY-MM-DD-<seq>.yaml` 자동 (seq = 그 날 ordinal).
- 박았으면 그 이름 검증 (regex) 후 사용.

## 5. 카탈로그 source

게시판 형태 + 그 외 일부 (gate_reject 측정용) 다 admit. 박을 때 name + url 만.

세 가지 입력 path:

1. **dashboard bulk paste** — `/candidates/<name>` detail 페이지 또는 `/candidates` index 의 "+ 새 catalog" 폼. textarea 에 URL N줄 paste (markdown / 문장 섞여도 URL regex 추출). backend 가 host 만으로 default name 채움 + cross-catalog dedup 검증 + 박음. title fetch X.
2. **Claude 가 yaml 직접 작성** — Claude 세션에서 `configs/candidates/<name>.yaml` 새로 작성. WebSearch 등 자동 발견 + curated name 박을 때.
3. **사용자 직접 편집** — git tracked, 손-편집 가능.

## 6. Batch driver — `scripts/register_batch.py` (N100 실행)

**위치**: N100 의 `scripts/register_batch.py`. dev 박스에서도 같은 파일 (git tracked) 이지만 *실행은 N100 만*.

**CLI** (rev5):
```
python scripts/register_batch.py
    [--catalog <name>]              # scope: catalog 1개 (파일명 stem) — repeat 허용
    [--url URL]                     # scope: URL 직접 — repeat 허용. catalog 무관.
    [--failed]                      # filter preset: rc ∈ {1, -1, -2, -3, -99}. 마커 자동 clear.
    [--rc <comma-list>]             # filter granular: 예 `--rc=1,-99`. 마커 자동 clear.
    [--force]                       # 마커/jobs 다 무시. 위 filter override.
    [--limit N]                     # enqueue 상한
    [--dry-run]                     # enqueue 안 함, 분포만
```

**scope axis**:
- `--catalog <name>` 1개 이상 (반복) — 명시 파일 합집합. 1개도 박혀야 (default 합집합 X — Q7).
- `--url URL` 1개 이상 (반복) — 직접 URL allowlist. catalog 안 있어도 OK (catalog 없이 URL 만으로 호출 가능).
- 둘 다 박혀도 OK (catalog 의 URL + 명시 URL 합집합).

**filter axis**:
- default = **untried** (jobs row 없는 entry 만).
- `--failed` = preset for retry-worthy: rc ∈ {1, -1, -2, -3, -99} (gen_fail + bug). 해당 slug 의 `.FAILED.json`·`.BUG.json` 마커 자동 삭제.
- `--rc <list>` = granular rc filter. `--rc=1` (gen_fail), `--rc=-99` (worker_exception) 등. 마커 자동 삭제.
- `--force` = 마커/jobs 다 무시. filter override (모든 entry enqueue).

**조합 예**:
- `register_batch.py --catalog=2026-05-20` → 그 catalog 의 untried (기본).
- `register_batch.py --catalog=2026-05-20 --failed` → 그 catalog 의 재시도 가치 실패.
- `register_batch.py --url https://… --url https://…` → 명시 URL 만.
- `register_batch.py --catalog=foo --force` → 다 (조심).

**rc**:
- 0 정상 종료
- 2 catalog load/검증 실패 (cross-catalog dedup 포함)
- 3 catalog 파일 없음
- 4 인자 오류

## 7. `scripts/remote.py batch-register` (rev5)

```
python scripts/remote.py batch-register
    [--catalog <name>]              # repeat 허용
    [--url URL]                     # repeat 허용
    [--failed]
    [--rc <list>]
    [--force]
```

내부: `ssh n100 'cd ~/notice-watcher && .venv/bin/python scripts/register_batch.py <args>'`. 인자 그대로 전달. 단 모든 인자는 사전 검증 (regex) 후 통과 — SSH injection 차단.

dashboard 에서 호출 X (rev5 정책). Claude 가 dev box 에서 직접 호출.

## 8. Dashboard `/candidates` — index + detail (rev5)

### 8a. `/candidates` (index)

좌측 nav 그대로. 페이지 구성:

1. **카탈로그 목록 표**:
   - 행별: name / entry 수 / registered N/M (진행률 bar) / fail mini-분포 (gen_fail/bug/reject 카운트) / 마지막 batch run 시각 / 클릭 → detail.
   - 정렬: name asc (default), 진행률 / 활동 정렬 토글.
2. **전체 KPI** (모든 catalog 합산):
   - registered / untried / pending / running / gen_fail / policy_reject / gate_reject / bug.
3. **"+ 새 catalog" 폼**:
   - name (옵션, 비우면 `auto-YYYY-MM-DD-<seq>` 자동) + bulk paste textarea (URL N줄).
   - submit → 새 yaml 파일 생성 + 정상 catalog 페이지로 redirect.

### 8b. `/candidates/<name>` (detail)

특정 catalog 의 entry 표 + KPI. rev4 §6 의 페이지 내용 (KPI / 분포 / 표 / 필터) 그대로. 추가/변경:

- **"+ URL 추가" 폼** (rev5 갱신) — single name+url 박는 form *대신* bulk paste textarea. 사용자가 URL N개 paste → host 만으로 name 자동 → 그 catalog 에 append. cross-catalog dedup 검증 (다른 파일에 있으면 reject 표시).
- **"▶ batch run" 버튼 / `/candidates/batch-run` route** — **제거** (rev5 정책).
- **상세 표** — rev4 의 행별 🗑 (catalog 안 remove) + slug 링크 (subs/jobs) 그대로.

**데이터 source**: `configs/candidates/<name>.yaml` ↔ N100 snapshot `bot.sqlite3` jobs (URL key LEFT JOIN). 별도 sqlite 폐기 (rev4 와 동일).

## 9. 코드 변경 목록 (rev4 → rev5)

**수정**:
- `scripts/register_batch.py` — multi-catalog load (여러 `--catalog` flag), cross-catalog dedup, `--failed` + `--rc=<list>` filter 추가, `--limit` / `--dry-run` 유지.
- `scripts/remote.py` `batch-register` — `--catalog` / `--failed` / `--rc` flag 추가, 인자 regex 검증.
- `dashboard/candidates_view.py` — index 라우트 추가 (`/candidates`), detail 라우트 (`/candidates/<name>`), bulk paste add 라우트 (`/candidates/<name>/add-bulk`), new-catalog 라우트 (`/candidates/new`). `/candidates/batch-run` POST 제거.
- `dashboard/templates/candidates.html` → `candidates_index.html` + `candidates_detail.html` 분리. bulk paste textarea + new-catalog form.

**마이그**:
- `configs/candidates/catalog.yaml` → `configs/candidates/legacy-2026-05-19.yaml` rename (`git mv`).
- `configs/candidates/2026-05-20.yaml` 신규 (rev5 grilling 직후 박는 ~100 entry).

**제거**:
- `dashboard/candidates_view.py` 의 `candidates_batch_run` POST route + `_html_escape` 의 의존성.
- `candidates.html` 의 "▶ batch run" form block.

## 10. 진행 워크플로 (이 세션)

1. CONTEXT.md catalog/catalog batch run split ✅
2. ADR 0004 robots.txt warn-not-block ✅
3. 이 doc rev5 (이 변경) ✅
4. catalog.yaml → legacy-2026-05-19.yaml `git mv`.
5. `configs/candidates/2026-05-20.yaml` 작성 (~100 entry).
6. `scripts/register_batch.py` 재작성.
7. `scripts/remote.py` batch-register 갱신.
8. `dashboard/candidates_view.py` + 템플릿 재작성.
9. probe_smoke pre-push 통과 → commit + push.
10. N100 pull → bot 재시작.
11. codex 리뷰.

## 11. 다음 세션 — 새 catalog 돌리기

사용자가 다음 세션 시작 시점:
- `configs/candidates/2026-05-20.yaml` ~100 entry 박혀 있음.
- dashboard `/candidates` → 카탈로그 목록 보임 (legacy / 2026-05-20 두 개).
- `/candidates/2026-05-20` 클릭 → 100 entry untried 표시.

batch run 트리거:
```
python scripts/remote.py batch-register --catalog=2026-05-20
```

drain 후 분포 분석 → fail_kind 별 hand-config / probe 개선 / engine fix → re-run `--failed` 로.

## 12. ADR 후보 (rev5 신규)

- **catalog multi-file partition** — 분류 의도 X, 편집 편의 split. 사용자 의도 박힘 (grill-with-docs Q6). hard-to-reverse 약 (revert 시 합쳐 단일 파일 가능). surprising 어느 정도 (rev4 → rev5 변경). real-trade-off 어느 정도 (단일 vs multi 의 git diff / 편집성). **별도 ADR 안 박음** — 이 doc 의 §2 가 reasoning 박음.
- **robots.txt warn-not-block** — ADR 0004 별도 박힘.

## 13. rev4 → rev5 변경 요약

| 영역 | rev4 | rev5 |
|---|---|---|
| 어휘 | "catalog batch" (단위/동작 conflate) | "catalog" (데이터) + "catalog batch run" (1회 실행) split |
| catalog 파일 | 1개 `catalog.yaml` | N개 `<name>.yaml`, 의미적 1 monotonic pool |
| dedup | within-file | within + cross-file |
| driver CLI | `--limit/--force/--url` | + `--catalog` (필수) + `--failed` + `--rc` |
| driver default scope | `catalog.yaml` 전체 | `--catalog` 명시 필수 (`--url` 만으로도 OK) |
| dashboard | 1 page (`/candidates`) | index (`/candidates`) + detail (`/candidates/<name>`) |
| dashboard run button | 있음 | 제거 (Claude/CLI 가 트리거) |
| dashboard add | name+url single form | bulk paste textarea (URL multi-line) |
| robots.txt | 동작만 기술 | ADR 0004 박혀 reasoning 문서화 |
