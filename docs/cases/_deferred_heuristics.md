# 보류한 휴리스틱 일반화 후보

case 처리 중 떠올랐으나 *지금* 박지 않은 probe/엔진 일반화 후보. 같은 패턴 1건 더 들어오면 임계량 도달 — 그때 휴리스틱화.

파일명 `_` prefix → `cases_index.py` 가 INDEX.md/DB 빌드 skip.

format: `- **<후보명>** — <신호> — 잡힐 case — 보류 사유 — 재검토 트리거 — 최초 commit`

새 후보 add: 한 줄 append. 박을 때 그 줄 삭제 + commit msg "deferred_heuristics 제거: <후보명>".

## 후보

- **[검토 완료 2026-05-17]** `_external_only_check` — 박지 않기로 결정. 신호 (`total≥1 AND ratio≥0.95`) 임계 보수화 (`total≥3`) 해도 잡히는 4 누적 케이스 (mdn/wiki-mirror/tistory/poly-pizza) 다 *이미 article_page_reject 인식기로 cover*. `total<3` 케이스 (github-wiki-see/poly-pizza) 는 호스트별 인식기 영역 (URL pattern 직접 등록) 이 정확. 휴리스틱 박는 가치 ≈ 0
- **[lifted 2026-05-17 commit `infra_multi_host_hub_reject`]** `_multi_host_hub_check` → `probe/extract.py:list_row_external_host` 의 `multi_host_hub` 필드 (≥3 unique external hosts AND ratio≥0.95) + `scripts/register.py:_multi_host_hub_check` 사전 거부 게이트. tistory root 자동 reject, poly-pizza FP 0%
- **[lifted 2026-05-17 commit `infra_pipeline_lift_round1`]** `cross_parent_aggregate_tile_pattern` → `static_vs_headless_check` rule 2 (selector-level diff) 로 박음
- **[lifted 2026-05-17 commit `infra_pipeline_lift_round1`]** `first_article_url_query_heavy_penalty` → `_article_url_score` 의 sort/filter/search/page/category query penalty 로 박음
- **[lifted 2026-05-17 commit `infra_pipeline_lift_round1`]** `cross_parent_aggregate_tile_pattern` → `static_vs_headless_check` rule 2 (selector-level diff) 로 박음
- **[lifted 2026-05-17 commit `infra_pipeline_lift_round1`]** `first_article_url_query_heavy_penalty` → `_article_url_score` 의 sort/filter/search/page/category query penalty 로 박음
- **[lifted 2026-05-26 commit `7e59fff`]** `archive_and_word_list_endpoint_penalty` → `_article_url_score` 에 `/archives?/` segment 와 `/<word>_list` ending 페널티 -3 박음. 누적 3건 (mobius `/news/archives/MM-YYYY` + comic-days `/info/archive/YYYY/MM/DD` + nexon-bluearchive sidebar `/bluearchive/board_list?board=N`) 봉합. mobius artifact replay 검증: NEW=`/news/patch-16-...` (실제 글) OLD=`/news/archives/06-2025` (archive)
- **`is_static_docs_site_first_article_anchor`** — `list_candidates.json` 의 `first_article_url` 의 fragment(`#anchor`) 떼고 보면 input URL 의 canonical 과 *같은 path 의 페이지* (자주 `index.html` 같은 hub) — same-page section anchor 또는 docs index. AND `nav_only_same_host=False` (`outside_nav>0`) 라 nav 게이트 통과. AND `article_meta_signals=None` (정적 docs 라 og:type 도 schema 도 없음). — sumo.dlr.de/docs (mkdocs material), 일반 정적 docs (Sphinx/Docusaurus/MkDocs) 가 잡힐 후보. **보류 사유**: 1건째 (sumo) 만 들어옴 — 호스트 명시 패턴(`article_page_reject.py`) 이 충분 + 휴리스틱 false positive 위험 (실제 보드인데 우연히 anchor link 가 첫 글로 잡힌 케이스 — 예: GitHub README hash-link 가 있는 보드). 재검토 트리거: docs site 같은 패턴 1건 더 들어오면 + multi-signal AND (anchor + sidenav cc≥20 + first_article path==input path) 안전하게 설계 가능할 때. — 최초 commit: 미정 (지금은 아이디어만)
- **`is_article_with_recommendation_widget`** — input URL 의 마지막 path segment 가 id-like (`\d{3,}` / `-\d+` / `[A-F0-9]{16,}` / `?id=<token>`) AND row 들이 input 의 *형제* (같은 path-prefix, 마지막 segment 만 다름) — `list_candidates.html_repeating_patterns` 의 sample_url 이 input path 의 parent + 다른 마지막 segment — iln-ieee/jobplanet 류 article 페이지 + 추천글 위젯 잡음 (nature 는 article_meta_signals 가 잡음). **보류 사유**: false positive 위험 큼 — `/board/123` 같은 *id-like board URL* (실제 board 인데 형식상 article 처럼 보임) 도 잡힐 가능성. URL pattern 단독 신호 위험성 사용자와 검토 (2026-05-17 dev box session). 재검토 트리거: iln/jobplanet 외 같은 패턴 1건 더 들어오면 + 안전한 multi-signal AND 설계 (예: row 들의 path 자식/형제 정밀 분류) 가능할 때. — 최초 commit: 미정 (지금은 아이디어만)
- **`root_marketing_category_suggestion_word_segment`** — REJECTED 사용자 안내 메시지 (`scripts/register.py:_root_marketing_homepage_check`) 의 카테고리 권장 URL 의 *첫 segment* 가 date (`/2026/`) 또는 hash-uuid (`/user41006705`) 같은 *비-board 패턴* 일 때, 두 번째 segment 또는 path 의 *워드* segment (`world`, `business`, `science` 등) 우선 추출. CNN root 케이스 ([[host_edition-cnn-com_root_82356c05]]) 에서 권장 URL 이 `/2026/` 로 나옴 — date hub 라 board 아님. **보류 사유**: 1건째 (CNN) 만 들어옴 — date-segment URL 권장은 *비-치명적* (사용자가 `/2026/` 시도해도 root_marketing 게이트 또는 board_shape 가 잡음). false positive (실제 사이트가 `/2026/` 가 진짜 board) 위험 낮지만 휴리스틱 가치도 낮음. 재검토 트리거: 같은 패턴 2건 더 들어오면 + 사용자 안내 quality 사용자 피드백 누적. — 최초 commit: `infra_root_marketing_homepage_gate_2026-05-19` (지금은 아이디어만)
- **`detect_xenforo_ips_platform`** — 정적 HTML 의 `<meta name="generator" content="XenForo">` (또는 IPS `data-controller="core.front..."` / `<body class="ipsApp">`) — Discourse 의 generator-meta 패턴과 동형. macrumors/head-fi/xda/nexusmods/hardforum/linustechtips/wordreference(XenForo)·invisioncommunity(IPS) 등이 root-marketing-hub 거부 또는 본문 fail 로 떨어진 다수 케이스가 잡힐 후보. **보류 사유**: Discourse 와 달리 공개 JSON API 없음 → `/whats-new/posts` HTML 스크레이프 손어댑터(XenForoAdapter/IPSAdapter) 신규 필요 (큰 작업). detect 휴리스틱만 박아도 어댑터 없으면 무의미. 재검토 트리거: XenForo/IPS 누적 ≥5 (이번 batch 만 ~7건) — 어댑터 작성 ROI 충분. — 최초 commit: 미정 (batch-2026-05-21-forums 에서 식별)
- **[lifted 2026-05-27 commit `<pending>`]** `selector_dot_escape_feedback` → `prompts/config_writer.system.txt` Tailwind utility class dot/colon escape rule + `engine/config_schema.py:_check_css_selector` retry feedback 으로 박음. etoland `ul.space-y-1.5` dot 미escape + outfit7 `:aspect-w-1` colon-variant leading pseudo-class 2건으로 임계 도달. case: `_generic_tailwind_class_escape_feedback`.
- **`static_variant_rows_not_promoted`** — `list_candidates.json` 는 head/script/meta 만 보고하지만 probe 의 S1 variant HTML(`s1.H4.html`) 에는 실제 row 반복(`div.story.homepage-post > a.story__text-link`) 이 존재. Indie Hackers root 에서 자동 생성은 posts_nonempty 0건으로 실패했지만 S1.H4 기준 손 config 는 baseline 30건. **보류 사유**: list 후보 소스 선택/승격은 `probe/extract.py` 또는 digest wiring 변경 필요, 이번 hard-stop allow-list 밖. 재검토 트리거: "정적 HTTP 충분" verdict 인데 list_candidates 후보 0건이고 S1 variant 파일에 same-host post/product 링크 반복이 있는 케이스 1건 더. — 최초 commit: 미정 (host_indiehackers-co_root_e4490db0 case)
- **`soft_404_title_page_not_found`** (P2) — JS 렌더 후 title/h1 이 "Page Not Found"/"없는 페이지"인데 그 not-found shell 의 nav 링크가 row-like 로 잡혀 `diagnosis.json` verdict 가 `soft_404` 아닌 "JS 실행 필요"로 오판 → gen_fail 로 샘. APA `/science/about/psa/` 1건 (probe 가 17 nav 링크를 row-like 로 셈, 실제론 not-found). **보류 사유**: (1) 1건째 — 이 batch 의 다른 404 는 전부 *hard* HTTP 404(store-crunchyro/animate/toranoana 원URL 등 = 이미 url_dead rc=4) 라 P2(soft, 200+not-found body) 는 APA 만. (2) "Page Not Found" 는 generic 영어구 → `_SOFT_404_PATTERNS` 본문 substring 매칭 시 정상 페이지 over-match 위험 (안전하려면 *title/h1 한정* + verdict JS-render 분기 *전* 검사). (3) APA 는 Incapsula anti-bot shell 도 끼어 신호 혼탁. 재검토 트리거: title/h1=not-found 인데 verdict≠soft_404 인 soft-404 1건 더 + title/h1 한정 안전 설계 가능할 때. — 최초 commit: 미정 (batch-2026-05-22 host_apa-org_science_e0556b0f, rejected)
- **`is_spa_shell_render_track`** — 정적 HTML 의 `<script src>` 가 `_app/immutable/`(SvelteKit) 또는 `_next/static/`(Next.js) 또는 `_nuxt/`(Nuxt) 패턴 ≥3 AND `list_candidates.html_repeating_patterns` 의 same-host cc≥5 cluster 가 ≤1 AND 그 1개 cluster 의 path 가 base URL path 와 같음 (즉 자기 자신 link) — 정적 HTML 이 SPA shell (사실상 빈 페이지). 카드·페이지네이션 모두 JS 렌더 후 DOM 에만 존재 → `_heterogeneous_hub_check` post-mortem 으로는 회복 불가, playwright_html re-probe + render 어댑터 권장. granbluefantasy.jp/news/ (SvelteKit, 정적 HTML 133줄 전부 `_app/immutable/*.css`·`*.js`, repeating pattern 1개 cc=7 `/ja/news/` = 자기 자신). **보류 사유**: 1건째 — 같은 패턴 1건 더 들어와야 일반화 트리거. 정적 신호로 false positive 위험 (heavy SPA 인데 actually 안 board) — render 권장 + SPA shell flag 까지만 박고 자동 retry 는 NO. 재검토 트리거: 같은 SvelteKit/Next/Nuxt no-static-content 사이트 1건 더 + classifier prompt 와 결합 안전 설계 가능할 때. — 최초 commit: 미정 (batch-2026-05-24-games-jp host_granbluefantasy_news_8cb478ee)
- **`framer_searchindex_sitemap_blog_list`** — Framer 또는 Next.js 로 만든 marketing/blog 사이트가 RSS 없을 때의 회복 후보 신호: (a) `<meta>` 의 `framer-search-index` → `https://framerusercontent.com/.../searchIndex-*.json` (path-key 별 `title`/`heading`/`text`/`url` 배열), (b) `/sitemap.xml` 의 `/blog/<slug>` URL, (c) Playwright DOM 의 `a.framer-text.framer-styles-preset-*` 카드 anchor (단 class hash 빌드별로 회전). gnosis.io/blog 1건 식별. **보류 사유**: (1) 1건째 — 같은 Framer/Next no-RSS blog 1건 더 들어와야 일반화 트리거. (2) searchIndex/sitemap 모두 `published_at` 또는 안정 sort 키 없어 "최신순 폴링" 보장 약함 — 알림봇 핵심 기능 (새 글 detect) 신뢰도 낮음. (3) Framer class selector 가 빌드 hash 라 장기 selector 취약. 재검토 트리거: 같은 패턴 (Framer/Next + no RSS + only sitemap/searchIndex) 1건 더 + sort 안정성 보강 (sitemap lastmod 신뢰 가능한 SaaS 식별 또는 searchIndex 의 hidden 날짜 필드 발견). — 최초 commit: 미정 (batch-2026-05-21-crypto, codex_card_layout_investigation_report 2026-05-23, gnosis.io/blog)
- **`wp_recognizer_502_immediate_cap_blocked`** — `scripts/register.py:2891-2904` 의 WordPress recognizer dispatch 가 `wp/v2/posts` 호출 시 **502 Bad Gateway / timeout / 429** 응답 → 현재는 "WordPress REST 폴백" 로그 + 일반 파이프라인 (LLM api_loop → agentic max_cycles) 으로 떨어져 *시간/quota 낭비 + 결국 cap_blocked*. WP REST API 가 영구 anti-bot 차단된 사이트 (zynga 케이스 — user browser `/news/` 도 502) 는 폴백해도 회복 불가능 → 즉시 cap_blocked `_save_rejected` 박고 종료. **보류 사유**: (1) 1건째 (zynga.com/news/) — 같은 WP-API-anti-bot 패턴 1건 더 들어와야 일반화 트리거. (2) WP API 일시 502 (cache miss / rate limit) 와 영구 차단 구분 신호 필요 (예: 3회 재시도 모두 502 + Cloudflare/Imperva 헤더). 재검토 트리거: 같은 WP recognizer 폴백 → agentic max_cycles → cap_blocked 패턴 1건 더 + 일시/영구 구분 신호 설계 가능할 때. — 최초 commit: 미정 (host_zynga-com_news_3aca3b50 case)
- **`vanilla_forums_subcommunity_directory_detect`** — `community.<name>.com/<locale>/api/v2/subcommunities` 형태의 Vanilla Forums API (`subcommunityID`/`folder`/`layoutViewType:"discussionList"`) + `/news/` 같은 root path → `302 → /<locale>/` locale redirect → community home 도착. home 자체는 *각 게임 = subcommunity = forum board* directory (게시판 아님). 분류기/게이트가 못 잡아 LLM 이 forum board 카드를 row 로 학습 → `probe_grounding_list_row_selector` fail → gen_fail. **보류 사유**: (1) 1건째 (community.king.com/news/) — Vanilla Forums community directory 패턴 1건 더 들어와야 일반화 트리거. (2) Discourse/Lemmy/Mbin recognizer 들과 같은 자리 (커뮤니티 platform detect) — Vanilla recognizer 신설 + classifier directory-page 신호 동시 필요. (3) Vanilla 의 subcommunity 안 *실제 board* (예: `/en/candy-crush-saga`) 는 또 별도 등록 자리 — directory 거부 + board 인식 분리. 재검토 트리거: 같은 Vanilla Forums subcommunity API 신호 1건 더 + directory vs board 분리 설계. — 최초 commit: 미정 (host_community-king-_news_f9079edd case)
- **`spa_shell_with_hr_aggregator_detail_click`** — 정적 HTML SPA shell (`<div id="app"></div>`, Vite/Vue/React 같은 frontend bundle) + Playwright detail click 시 host 변경 (`app.mokahr.com` / `careers.<name>.com` / `<workday>.com` 등 HR/채용 aggregator) → list host ≠ detail host → `agent self-veto(non_board): host mismatch — 검색결과/aggregator` 자동 거부 (rc=3 REJECTED). 게임/SaaS 회사의 `/news/` 또는 `/careers/` 가 marketing landing 인데 actual content 가 외부 aggregator. agent self-veto 흐름이 *이미* 자동 처리 — 추가 로직 X. **보류 사유**: 자동 처리 잘 됨 (yo-star.com/news/ 케이스 — `register.py --reuse-probe` 1회로 깔끔 거부). 일반화 가치 = probe heuristic 으로 *probe 시점에* detect 해서 LLM 호출 자체 skip (cost saving). 재검토 트리거: 같은 SPA-shell + HR aggregator detail click 패턴 3건 누적 + probe-time detect 안전 설계 (Playwright click 이 probe 단계에 있어야). — 최초 commit: 미정 (host_yo-star-com_news_8f81136f case)
- **`probe_simplify_huge_atomic_class_selector_with_url_pattern_fallback`** — `list_candidates.html_repeating_patterns[].selector` 가 (a) >300 chars 거대 atomic-class chain 이거나 (b) bracket-arbitrary token (`[&_*]:...` / `[&.<state>]:...`) 또는 `(...)` 함수형 토큰 (`gap_token(spacing.d.24)`) 포함 — Panda CSS / Chakra v3 / Park UI / 기타 utility-first CSS-in-JS framework 산물. soupsieve 가 못 parse 하고 LLM 도 verbatim 사용 불가능. 같은 cluster sample URL 들이 path prefix 공유 (`/news/<slug>`, `/blog/<slug>`) 하면 *simplified fallback* (parent stable class + `a[href^='<prefix>/']` 또는 `tag[href^='<prefix>/']`) 을 *추가* selector 후보로 emit. **보류 사유**: 1건째 (metacoregames `<feed-block-items-grid>` + Panda CSS `[&_img]:trf_scale3d(1,1,1)_translate3d(0,0,0)` 등 600+ char selector + `/news/<slug>` 공유 prefix). simplified fallback false positive 위험 (nav/footer link cluster 도 같은 prefix 가질 수 있음) — parent stable-class anchoring 필수. 재검토 트리거: 같은 atomic-class + bracket-arbitrary 패턴 1건 더 (Chakra v3 / Panda / Park UI / TailwindCSS arbitrary-value `[mask-position_*]:`) + simplified fallback 안전 설계 (parent 유일성 검증 / row count threshold ≥3). — 최초 commit: 미정 (host_metacoregames-c_news_450fe577 case, batch-2026-05-24-games-mobile-casual)
  - **`probe_verdict_promote_render_when_raw_anchor_zero`** — probe diagnosis `verdict: 정적 HTTP로 충분` 인데 raw httpx variants (`s1.H2.html` / `s1.H4.html`) 안 `list_candidates.first_article_url` 도메인 매칭 anchor 가 0 개 OR cluster.sample_url prefix 매칭 anchor 가 0개 — 즉 정적 HTML 은 shell 만 + 실제 row 는 hydration 후. 현재 verdict 가 잘못 판정해 LLM 이 httpx_html strategy 만들고 fetch_list 0건 fail. probe 가 *raw vs rendered anchor count 차이* 신호로 verdict 를 `playwright_html 필요` 로 자동 교정 + diagnosis.recommended_strategy 갱신. **보류 사유**: 2건째 잠정 (granbluefantasy SvelteKit 1건 + metacoregames Next.js+Panda 1건 = 다른 framework 같은 행동). 단 `is_spa_shell_render_track` (정적 script src 패턴) 와 신호 source 다름 — 이건 *anchor count delta* 기반이라 framework agnostic. 두 deferred entry 통합 검토 후 lift. 재검토 트리거: 같은 raw-anchor-zero + rendered-anchor-many 패턴 1건 더 + 통합 설계 (script-src signal + anchor-delta signal 같이 봄). — 최초 commit: 미정 (host_metacoregames-c_news_450fe577 case, batch-2026-05-24-games-mobile-casual; granbluefantasy 와 catalog)
- **`inline_var_list_plus_orb_blocked_script_signal`** — 정적 HTML 에 `var <name>_list = '{...}'` 같은 inline JSON list payload 가 크고, 동시에 cross-origin `.js` script 가 Chrome ORB 또는 HTML-body challenge 로 막혀 후속 inline script 실행이 중단됨 → Playwright rendered DOM row 0건. AFK Journey `/news/` 는 inline `var news_list` + `dapcdn.63cj.com/common-utils/index.1.1.7.umd.js` text/html/HTML challenge 때문에 Vue hydration script append 가 멈춤. **보류 사유**: 1건째 + probe/extract.py 변경은 이번 ORB route hook task 범위 밖. 재검토 트리거: inline list payload 는 있는데 rendered rows 0이고 console/requestfailed 에 ORB 또는 `.js` text/html 신호가 있는 케이스 1건 더. — 최초 commit: 미정 (host_afkjourney-farl_news_2382ee85 case)
- **`css_module_hashed_class_attr_contains_rewrite`** — `probe/extract.py` 의 row pattern emitter 가 클래스 fragment 형태 `_<word>_<short-hash>` (Svelte/CSS-modules build-hash 패턴 — 예: `_newsCards_1stmd_22`, `_newsCard_1stmd_22`, `_newsCardContent_1stmd_65`) 를 detect 하면 attr-contains form `[class*="_<word>_"]` 후보를 같이 emit. LLM/agentic 이 *어느 쪽이든* 골라도 build redeploy 후 hash 변동으로 selector 가 깨지지 않음. 보너스: 동반 발생한 `_size:heading_<hash>` 같은 unescaped-colon hashed fragment 도 attr-contains 형으로 자동 회피 (soupsieve pseudo-class 컴파일 에러 회피). **보류 사유**: 1건째 (pokemongo.com /news Niantic Svelte). attr-contains 가 너무 너른 selector 가 될 risk — same-prefix 다른 컴포넌트 (`_newsCardContent_`, `_newsCards_`) 혼동 가능. 재검토 트리거: 같은 CSS-modules build-hash + redeploy-after-probe 케이스 1건 더. — 최초 commit: 미정 (host_pokemongo-com_news_002609a9 case, batch-2026-05-28-games-gacha-global-02)
- **`post_only_json_api_session_bound`** — `probe/discover.py` JSON candidate emitter: HAR 에서 POST endpoint 가 풍부한 JSON list (information_list / posts / items …) 를 반환했고 같은 URL 의 standalone GET 또는 raw-headers POST 가 짧은 sentinel ({"response_code": <N>} 형 ≤30 byte body) 를 돌려주면 `api_session_bound=true` 플래그 + `render_required=true` 를 list_candidates 의 해당 후보에 박는다. config_writer prompt 가 이 플래그 보면 `playwright_html` 로 strategy 고정 + 사이트 root 에서 wait_selector 잡음. **보류 사유**: 1건째 (umamusume.com /news Cygames Svelte). 휴리스틱 신호 정의 (sentinel body length threshold, response_code field name 변종) 가 아직 모호. 재검토 트리거: POST-only session-bound JSON API 사이트 1건 더. — 최초 commit: 미정 (host_umamusume-com_news_074ee55a case, batch-2026-05-28-games-gacha-global-02)
- **`httpx_json_post_method_support`** — `engine/strategies/httpx_json.py` 에 `request.method=POST` + `request.body=<obj|str>` + `request.json=<obj>` 옵션 추가 (현재 GET 전용 — `get_with_tls_fallback`). F-layer 후보. **보류 사유**: umamusume.com 같은 session-bound POST 는 이거만으로는 못 뚫음 (cookie/consent 게이트). session-free POST-only API 가 있는 사이트가 있어야 ROI 발생. 재검토 트리거: POST 만 받는데 standalone POST 가 *session 없이도* 본문을 돌려주는 사이트 1건 들어오면. — 최초 commit: 미정 (host_umamusume-com_news_074ee55a case 에서 식별, 이번 PR 에는 박지 X)
- **`riot_nextjs_news_data_testid_recognizer`** — Riot Next.js news pages on `*.leagueoflegends.com` expose a stable list DOM `section[data-testid="article-card-grid"] a[data-testid="articlefeaturedcard-component"]`; host-local config only needs `url_template` locale path, `Referer`, urljoin base, and the shared `card-title`/`time@datetime`/`card-description` fields. **보류 사유**: positive cluster is TFT(done) + Wild Rift(handcrafted) = 2; four nearby Riot endpoints in this batch were gate_reject/url_dead, so recognizer blast radius needs one more positive live site or a tighter host/path gate. 재검토 트리거: 4+ positive Riot Next.js news endpoints or another `*.leagueoflegends.com/<locale>/news/` hit with the same DOM. — 최초 commit: 미정 (batch-2026-05-28-games-gacha-global-05, host_wildrift-league_news_8313486f)
