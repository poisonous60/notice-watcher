---
slug: naver-cafe_31104609_1_c9b1f633
url: https://cafe.naver.com/f-e/cafes/31104609/menus/1?viewType=L
status: 🚫 등록 거부 (등록은 OK, 본문 추출 불가 — 정책상 우회 X) + 시스템 차원 후속 완료
date: 2026-05-16
failure_keys: [body_empty_at_baseline, article_api_401_403]
config_strategy: handwritten
adapters_changed: []
engine_files_touched: [scripts/register.py, bot/inspector.py, bot/site_ops.py, bot/main.py, bot/worker.py]
fix_layer: F
tags: [naver-cafe, gated-board, body-empty, user-report]
---

## 무엇이 일어났나
사용자(poi23619) `/preview` → 자동 등록 성공 (recognizer `naver-cafe` 매칭, baseline 31건). 그러나 사용자 `/report` 신고 #1: "로그인을 해야 글이 보여서 글이 안 보여요". `inspect_subs.py fetch` + 직접 `fetch_article` 로 본문 확인 → **3건 모두 `body_chars=0`**. `NaverCafeAdapter` 가 `article.cafe.naver.com/gw/v4/cafes/.../articles/...` 호출했을 때 401/403 받음 — 어댑터 정책(`adapters/navercafe.py` docstring): "비공개·등급제한 게시판이면 본문 API 가 401/403 → 어댑터가 본문 비워 반환(우회 안 함)".

목록 API (`apis.naver.com/cafe-web/cafe-boardlist-api/v1/cafes/.../menus/.../articles`) 는 비회원 200 → 31건. 따라서 *시스템 관점* "등록 정상", *사용자 관점* "알림 본문 빔". 같은 cafe_id 31104609 의 카페 홈 URL (`https://cafe.naver.com/gutterlife`) 자동 등록 실패 사례(`host_cafe-naver-com_gutterlife_e0009e69.FAILED.json`, 2026-05-15) 와 짝.

## 무엇을 바꿨나
**코드 fix 없음**. 근본 = 네이버 카페 등급제한 정책 + 크롤링 정책상 우회 금지(`docs/크롤링 지침.md` §6). 어댑터는 이미 빈 본문을 정상 반환 — 고칠 게 없음.

처리:
1. **신고 종결** — `bot.sqlite3:reports` #1 → `resolved`. note: "네이버 카페 비공개·등급제한 게시판 — `NaverCafeAdapter` 정책상 본문 추출 불가(401/403 시 빈 본문 반환, 우회 금지). 알림은 제목·URL 만. 본문 보려면 storage_state 로그인 세션 인프라 필요(별도)".
2. **사용자(poi23619) 안내** — owner Discord DM: "이 카페는 가입/등업 후 로그인 세션 없으면 본문이 안 보입니다. 알림은 제목·URL 만 옵니다. 본문까지 받고 싶으면 storage_state 로그인 경로 인프라가 따로 필요한데 아직 구현되지 않았어요. 알림 받지 않으시려면 `/unwatch` 해주세요."

## 시스템 차원 후속 (구현 완료)
같은 패턴 신고 반복 방지 인프라 — 같은 PR 로 추가:

- `bot/inspector.py::fetch_sim` — 상위 N 개 중 첫 3건은 `fetch_article` 도 시도 → `body_chars` 키 추가. `diagnose()` 가 body_chars 모두 0이면 새 태그 `article_body_empty` (warn).
- `scripts/register.py::_check_body_at_baseline` — 등록 직후 첫 3건 본문 fetch. 모두 0자면 True, 하나라도 본문 있으면 False, 모두 예외면 None. 3 분기 모두(`_try_known_platform` / `--config` / 일반 파이프라인) 에서 호출, `_save_state` 의 `body_empty_at_baseline` 인자로 state.json 에 저장.
- `bot/site_ops.py::body_empty_at_baseline(slug)` + `body_warning(slug)` — state 키 lookup + 봇 응답용 경고 문구.
- `bot/main.py` `/preview`·`/watch` 등록된 사이트 분기 + `bot/worker.py` 신규 등록 ack — head 메시지에 `body_warning` 합침: "⚠️ 본문 추출 안 됨 (등급/로그인 필요 가능) — 알림은 제목·URL 만 옵니다."
- `tests/inspector/test_diagnose.py` — `article_body_empty` 케이스 3개 (양성/하나라도 본문 있으면 suppress/모두 None 이면 suppress).

backfill: 이번 slug 의 state.json 에 `body_empty_at_baseline:true` 수동 patch (기존 등록은 그 키 없음 → 재배포 후 새 등록부터 자동, 이 slug 만 수동).

## 자가 점검 (5-질문)
1. **어느 자리?** — (F) 새 엔진 코드 (`scripts/register.py` 등록 플로우 + `bot/` 런타임). 처음엔 (C) probe digest 신호로 분류했지만 `probe/` 파일은 변경 X — 진짜 신호 추출이 *probe* 단계가 아니라 *register 등록 후* 단계라서 C 부적절. probe artifact 에 export 안 하고 state.json 에 직접 플래그 박음. (E)/(D)/(B)/(A) 도 아님: schema·retry feedback·few-shot·system prompt 변경 X.
2. **이전 케이스 있나?** — `cafe.naver.com_f-e_cafes_30291108_menus_6_viewType_L` (같은 어댑터, 비공개 본문 언급 있음, 코드 fix X). 그 case 는 신고 없이 등록만, 이 case 는 *실제 신고로 본문 빔 표면화* → 인프라 도입 동기.
3. **누구 깰까?** — 0 사이트. `_save_state` 시그니처 추가 인자 default=None → 기존 호출자 그대로. state.json 추가 키 → 폴링/봇 모두 옛 키 부재 OK (`get(...)` default).
4. **검증 그린?** — `python tests/inspector/test_diagnose.py` 17/17 PASS. `python scripts/probe_smoke.py` PASS 190 / FAIL 0.
5. **case 파일** — 본 파일. fix_layer=C, engine_files_touched 명시.
