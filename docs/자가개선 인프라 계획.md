# 코딩 에이전트 파이프라인 개선 계획

> 작성 2026-05-15 (rev 3 — audit-1·2·3 후 minimal 재설계). 트리거: NTE 케이스(host_nte-perfectworl_kr_c8f4855a) 손-config 처리 후 "hand-config 가 매번 잘 작동하는 구조인가" grill-me.
> 합의: **외부 인프라 X — Claude 의 thinking step + cheap review subagent + per-case 메모리 + 별도 정기 rot-review SKILL**. 
> 실행: Claude Code 자율(여러 세션 누적). 각 phase 끝에 사용자 확인.

---

## 0. 배경

### 0a. 무엇이 문제인가
자동 등록 파이프라인(probe → digest → Gemini → config JSON → engine)이 실패한 사이트는 사람 + Claude Code 가 손-config 한다. 이 과정에서 Claude 는 *자율적으로* probe/prompt/schema/코드를 손댈 수 있는데:

1. **버그 위험** — 자율 변경이 21개 기존 사이트 회귀 일으킬 수 있음.
2. **누더기 위험** — 같은 종류 실패에 매번 다른 자리에 박으면 시스템 일관성 깨짐.
3. **prompt rot** — `prompts/config_writer.system.txt` 의 룰이 시간 따라 죽거나 중복.
4. **단일 doc 의존** — `docs/사이트별 등록 시도 기록.md` 한 파일에 모든 케이스 → grep 어려움 + 병합 충돌 + rot 검출 안 됨.

### 0b. 이전 plan(audit-1·2·3) 의 교훈
원안(plan v1 — 4주 인프라: tests/sites stage 4 + llm_eval + INDEX.json + cap + self-verification)·v2(reviewer 추가 + R-XYZ + per-case) 둘 다 audit 가 깬 한계:
- **tests/sites stage 4** = raw HTML 캡처 메커니즘 침묵 (validate.py 가 raw 폐기) + adapter 의 절반 fetch/parse 분리 안 됨 → 작동 X
- **llm_eval** = 비결정 + model deprecate 감지 미정의 + cost cap silent break → 신뢰 0
- **INDEX.json** vs 기존 md = 중복 rot 면적 2배
- **layer rubric 강제** = reviewer subagent 의 Bash 미검증 + self-modification 미tigated → theater
- **R-XYZ + grep rot detection** = `prompt_sections_relevant` 필드 optional → 빈 필드 천지 → false-positive rot 천지 → DOA
- **§7 trigger thresholds (30+/5+)** = 도달까지 2-5개월 → 인프라 ROI 부족
- **"무인" 약속** = branch protection·model rotation·--no-verify 가드 다 user/behavior 의존

### 0c. v3 의 핵심 통찰

**prompt 변경의 *두 종류* 를 분리**:

| 종류 | 누가 | 데이터 흐름 |
|---|---|---|
| **prompt 추가 (additive)** — 새 패턴 → 새 룰 한 줄 | hand-config 에서 자율 (필요 시) | 그 시점 *케이스 기록* 으로 충분 |
| **prompt 개선 (curative)** — 죽은 룰 제거 / 중복 머지 / 모순 정리 | 별도 SKILL `prompt-rot-review` (장래) | 누적 cases 분석 필요 |

이 분리로:
- hand-config = *지금 만들 데이터* 만 만들면 됨 (case 파일)
- prompt-rot-review SKILL = *누적 데이터를 능동적으로 분석* — 빈 필드 있어도 SKILL 이 추론
- audit-3 #1 (R-XYZ rot detection DOA) 자연 해소 — 그 메커니즘 hand-config 책임 아님

### 0d. 다섯 자리 (fix-knowledge layers) — 재확인

| 자리 | 압력 시점 | 비용 |
|---|---|---|
| **(E) schema 거부** | config 생성 직후 | 낮음, hard |
| **(D) retry feedback** | 1~4차 사이 | 낮음 |
| **(C) probe digest 신호** | probe 단계 | 높음 (휴리스틱 + fixture + contract) |
| **(B) few-shot** | 매 prompt | 토큰 비쌈 |
| **(A) system 규칙 *추가*** | 매 prompt | 추가만 OK, 수정/제거 X (별도 SKILL) |
| **(F) 새 엔진 코드** | 코드 자체 | 큼, sub-카테고리 4종 |

원칙: 싸고 hard 한 쪽부터. (A) 추가 가능, (A) 수정/제거 는 hand-config 영역 아님.

---

## 1. 핵심 설계

### 1a. hand-config 사이클 (매 케이스)

```
1. /report 또는 /preview 실패 → Claude 모드 B 시작
2. 진단 + 픽스 시도 (E/D/C/B/A/F 자율, A 는 *추가* 만)
3. SKILL.md 5-질문 자가 점검 — 가이드라인, 권장
   ① 어느 자리? ② 이전 케이스? ③ 누구 깰까? ④ 검증 그린? ⑤ 결정 메모
4. docs/cases/<slug>.md 작성 (frontmatter 일부 필수)
5. python scripts/cases_index.py — INDEX.md 갱신
6. main thread 가 python scripts/probe_smoke.py 실행 — 결과 캡처
7. Agent(subagent_type='hand-config-reviewer', model='sonnet') 호출
   - main thread 가 diff + case 파일 + smoke 결과 prompt 에 박음
   - reviewer 는 *판단만* (Bash 없어도 작동)
   - PASS / FAIL 보고
8. FAIL → user 에게 보고. user 가 결정 (자동 재호출 X)
9. PASS → git commit + push
10. pre-push hook = probe_smoke 한 번 더 (안전망)
11. N100 ssh 배포
```

### 1b. 별도 SKILL `prompt-rot-review` (장래, *지금 안 만듦*)

```
사용자가 명시 호출 (예: "prompt rot 점검")
→ SKILL 이 prompts/ 전체 + docs/cases/*.md 전수조사
→ 죽은 룰 / 중복 / 모순 / 과spec 진단 보고
→ 사용자 승인 후 prompt 수정
```

트리거 조건: case 50+ 누적 또는 prompt 200줄+ 또는 사용자 직감.

### 1c. 메커니즘

| 메커니즘 | 자리 | 비용 |
|---|---|---|
| **1. SKILL.md 5-질문 자가 점검 (가이드)** | `.claude/skills/hand-config/SKILL.md` 본문 | 0 |
| **2. `hand-config-reviewer` subagent (Sonnet)** | `.claude/agents/hand-config-reviewer.md` | 호출당 ~$0.04 |
| **3. `docs/cases/<slug>.md` per-case 파일** | `docs/cases/` | 케이스당 ~5KB |
| **4. `scripts/cases_index.py`** | 스크립트 1개 | 30분 작성 |
| **5. `.git/hooks/pre-push` = probe_smoke 만** | hook + setup | 0 |

---

## 2. Phase 별 작업

### P0 — docs rot sweep

audit-2(q) 가 짚은 잔존. v1 commit (4f6fa6e) 은 SKILL.md 만 픽스했음.

- `docs/config 기반 엔진 가이드.md` line 67, 71, 233 — `known_platforms.py` → `engine/recognizers/`
- `docs/config 자동생성 실패 케이스.md` line 90 — 동일
- `docs/사이트별 등록 시도 기록.md` line 54 — P1 에서 마이그 후 archive

추정: 30분.

### P1 — Per-case 파일 시스템

#### P1-a. `docs/cases/<slug>.md` 스키마 (필수 최소)

```markdown
---
slug: host_nte-perfectworl_kr_c8f4855a
url: https://nte.perfectworld.com/...
status: 🔧 손config              # ✅자동 / 🔧손config / 🧩손어댑터 / ❌FAILED / 🚫거부
date: 2026-05-15
requested_by: poi23619           # 선택 — /report·/preview 요청자
failure_keys: [article_body_len] # 선택 — [FAIL] <체크> array. 자동 등록인 경우 비움
fix_layer: E                     # 선택 — 명확하면 박음, ambiguous 면 비움. string OR array
fix_pr: e670930                  # 선택 — git short hash
config_strategy: httpx_html      # 선택
prompt_sections_relevant: []     # 선택 — 비워둬도 OK, rot-review SKILL 이 추론
engine_files_touched: []         # 선택 — F-layer 식별
tags: []                         # 선택 — 자유 분류 ("cloudflare", "spa" 등)
---

## 무엇이 일어났나
[자유 markdown]

## 무엇을 바꿨나
[자유 markdown — 5-질문 답 요약 추천]

## 회귀 검증
[손-실행 결과 / probe_smoke 결과]
```

**필수 (4)**: `slug`, `url`, `status`, `date`
**선택 (8)**: 위 나머지

원칙: ambiguous 면 비워둠. 강제 통제 X. rot-review SKILL 이 누적 데이터에서 추론.

#### P1-b. `scripts/cases_index.py`

- PyYAML (확인됨: 6.0.3 설치 — audit-3 (a))
- `docs/cases/*.md` frontmatter 파싱 → `docs/cases/INDEX.md` 덮어씀
- 표 컬럼: slug · status · date · fix_layer · failure_keys · url
- footer: layer 분포, strategy 분포, 최근 90일 케이스 수
- idempotent
- `--check` 옵션 안 만듦 (audit-3 (k) UX friction)

#### P1-c. 기존 `사이트별 등록 시도 기록.md` 마이그

8 entries → `docs/cases/*.md` 8개. retroactive 판단 (audit-3 (f)) 피하려면:
- **fix_layer 비워둠** (이전엔 명시 안 했으니 추론 시도 X)
- 본문은 기존 prose 그대로 복사
- frontmatter 는 명백한 필드 (slug, url, status, date) 만

기존 md 운명: **archive** — 헤더에 "⚠ 2026-05-15 이후 deprecated. 새 케이스는 `docs/cases/`. 이 파일 역사적 기록 보관용." 명시. 새 entry 추가 X.

추정: 2-3시간.

### P2 — SKILL.md 5-질문 *가이드라인*

#### P2-a. 추가 섹션 — *권장*, 강제 X

```markdown
## 자율 개선 시 자가 점검 (가이드라인, 권장)

probe/prompt/schema/코드 손대기 전 다음 다섯 질문에 답해보면 누더기 위험 줄어든다 — case 파일 본문에 또는 commit msg 본문에:

1. **어느 자리?** — (E) schema / (D) retry feedback / (C) probe 휴리스틱 / (B) few-shot / (A) system 규칙 추가 / (F) 새 엔진 코드. 
   원칙: 위에서부터 차례로 따져 첫 매칭. (E)/(D) 로 잡힐 것을 (C)/(A) 에 박지 X. ambiguous 면 비워두고 진행.
   ※ (A) system 규칙은 *추가* 만 OK. 기존 룰 수정/제거는 별도 SKILL `prompt-rot-review` 영역.

2. **이전 케이스 있나?** — `grep -l '<failure_key 또는 root_cause>' docs/cases/*.md` 또는 `cat docs/cases/INDEX.md`.

3. **누구 깰까?** — 21 configs 중 영향 사이트 enumerate. 0개 가능하지만 *왜 0개인지* 한 줄 적기 (audit-3 (n) 회피).

4. **검증 그린?** — `python scripts/probe_smoke.py` + 영향 사이트 손-실행.

5. **결정 메모** — case 파일 작성 + commit msg `[fix-layer: E|D|C|B|A|F|none] <slug>` prefix (선택).

위 다섯 답이 없어도 commit 막지 X — 그저 *생각해보면 좋은* 질문. 진짜 검증은 reviewer subagent + pre-push hook.

## 자가 review (commit 직전 — *권장*)

`Agent(subagent_type='hand-config-reviewer', model='sonnet')` 호출 — 변경 diff + case 파일 + probe_smoke 결과를 prompt 에 박아 넘김. reviewer 가 위반만 한 줄씩 보고. FAIL → user 에게 알리고 결정 받음 (자동 재호출 X).
```

#### P2-b. `.claude/agents/hand-config-reviewer.md`

```markdown
---
name: hand-config-reviewer
description: hand-config 변경 검토 — 5-질문 가이드 준수, fix-layer↔file 정합성, case 파일 유효성, 외부 검증 결과 (probe_smoke / 손-실행) 확인
model: sonnet
tools: Read, Grep, Glob
---
# 역할
notice-watcher 의 hand-config 변경 검토. main thread 가 변경 diff·case 파일·probe_smoke 결과를 prompt 로 넘김. Bash 없음 — 실행은 main thread 책임.

5-질문 가이드는 `.claude/skills/hand-config/SKILL.md` 의 "자율 개선 시 자가 점검" 섹션 — Read.

# 검증 항목 (모두 PASS 시 PASS, 위반 있으면 FAIL + 위반만 한 줄씩)

1. **case 파일 존재** — `docs/cases/<slug>.md` 있나? frontmatter 필수 4 필드 (slug, url, status, date) 채워져있나?

2. **fix_layer 정합성 (박혀있을 때만)** — declared layer 와 변경된 파일이 맞나?
   - E → engine/config_schema.py 변경
   - D → prompts/config_writer.retry_skeleton.txt 또는 scripts/register.py 의 feedback 빌더
   - C → probe/ + tests/probe_heuristics/
   - B → generate/prompt.py 의 _EXAMPLE_CONFIG_FILES 또는 configs/
   - A → prompts/config_writer.system.txt (*추가* 만 — 기존 줄 수정/제거면 FAIL)
   - F → engine/strategies/ 또는 adapters/ 등
   fix_layer 비어있으면 이 검증 skip — OK.

3. **회귀 검증 흔적** — case 파일 본문 "회귀 검증" 섹션 또는 probe_smoke 결과 prompt 에 있나? "0 sites affected" 만 적혀있고 *왜 0인지* 설명 없으면 FAIL (audit-3 (n)).

4. **prompt 수정 종류** — 만약 `prompts/config_writer.system.txt` 변경됐다면:
   - *기존 줄 수정/제거* 면 FAIL — 별도 SKILL `prompt-rot-review` 영역
   - *새 줄 추가* 만 OK

5. **외부 검증 결과** — main thread 가 넘긴 probe_smoke 출력에 FAIL/exit-1 있으면 FAIL

# 출력 형식
PASS
또는
FAIL
- <위반 1>
- <위반 2>
...

작문 X. 위반만.
```

추정: 1-2시간.

### P3 — pre-push hook

`.git/hooks/pre-push`:
```bash
#!/bin/sh
echo "[pre-push] probe_smoke ..."
python scripts/probe_smoke.py
status=$?
if [ $status -ne 0 ]; then
  echo "[pre-push] FAIL — push 차단. --no-verify 금지."
  exit $status
fi
```

`scripts/setup-hooks.{sh,ps1}` — `.git/hooks/pre-push` 복사 + chmod +x. `docs/운영 메모.md` 의 dev 박스 setup 섹션에 등록.

cases_index 자동 실행은 *안 함* (audit-3 (k) UX friction). 사용자/Claude 가 case 파일 작성 시 직접 `python scripts/cases_index.py` 실행. reviewer 항목 1 이 case 파일 존재 검증.

추정: 30분.

### P5 — `prompt-rot-review` SKILL (장래, *지금 안 만듦*)

명시적으로 *deferred*. 트리거 조건:
- `docs/cases/*.md` 가 50건+ 누적
- 또는 `prompts/config_writer.system.txt` 가 200줄+
- 또는 사용자가 prompt 누더기 느낌 → 직접 SKILL 호출

그때 별도 plan 수립. 지금 결정 X.

---

## 3. 미결 결정의 기본값

| 결정 | 기본값 |
|---|---|
| frontmatter 필수 필드 | slug·url·status·date 만. 나머지 다 선택 (ambiguous 면 비움) |
| fix_layer string vs array | string. multi-layer 면 array OK. 강제 통제 X |
| 기존 md 운명 | archive (deprecated 헤더, new entry 금지). 폐기 X |
| reviewer 모델 | sonnet |
| reviewer Bash | 없음 — main thread 가 probe_smoke 실행 후 결과 prompt 에 박음 |
| reviewer FAIL 처리 | user 에게 보고 — 자동 재호출 X (audit-3 (i) 자기모순 회피) |
| pre-push hook 우회 | `--no-verify` 금지 (system prompt 가드 + reviewer 항목 5) |
| cases_index --check | **안 만듦** (audit-3 (k)) |
| R-XYZ HTML 주석 | **안 만듦** (audit-3 #1 — rot detection 책임 분리) |
| GH Actions / N100 runner | **안 만듦** (free tier 보존 + N100 부담 회피) |
| prompt-rot-review SKILL | **장래** — 트리거 도달 시 별도 plan |

---

## 4. Done condition

| phase | done |
|---|---|
| P0 | 3 docs 의 `known_platforms.py` 죽은 참조 0 |
| P1 | `docs/cases/*.md` 8개 (마이그). `scripts/cases_index.py` 실행하면 INDEX.md 생성. 기존 md archive 명시 |
| P2 | `.claude/agents/hand-config-reviewer.md` 있음. SKILL.md 5-질문 가이드 + reviewer 호출 step 추가 |
| P3 | `.git/hooks/pre-push` 동작. `scripts/setup-hooks.{sh,ps1}` 있음 |

전체 done: 다음 hand-config 케이스가 위 사이클로 한 번 무인 돌면 성공. **단 "무인" = Claude 가 user 와 협력해 도는 모드 — 사람 눈 매번 거치는 게 안전망**.

---

## 5. 비용

| 항목 | 비용 |
|---|---|
| P0 sweep | 30분 |
| P1 case 시스템 + 마이그 | 2-3시간 |
| P2 SKILL + agent | 1-2시간 |
| P3 hook | 30분 |
| **총 구현** | **4-6시간** |
| reviewer 호출 (Sonnet 4.6) | ~$0.04/call × 10 케이스/월 × 1.5회 ≈ $0.6/월 |
| pre-push 실행 | 0.4초/push — 무시 |

---

## 6. 회피할 함정

audit-1·2·3 의 모든 critique 가 어떻게 처리됐나:

| critique | v3 처리 |
|---|---|
| **audit-1·2 tests/sites stage 4** | 안 만듦 (제거) |
| **audit-1·2 llm_eval** | 안 만듦 (제거) |
| **audit-1·2 INDEX.json 중복** | INDEX.md 가 `docs/cases/*.md` 의 *derived view* — 단일 source-of-truth |
| **audit-1 layer cap** | 안 만듦 — prompt-rot-review SKILL 영역 |
| **audit-2 reviewer Bash 미검증** | reviewer 가 Bash 안 받음. main thread 가 실행 후 결과 넘김 — Bash 부여 안 받아도 작동 |
| **audit-2 F7 reviewer self-modification** | 약화 (강제 X) — user 가 매번 audit 돌리는 패턴이 사람 눈 안전망 |
| **audit-2 trigger threshold 자기파괴** | 트리거 안 짜 — P0~P3 다 *지금* 가치 있음 |
| **audit-3 #1 prompt rot DOA** | rot detection 책임 분리 — hand-config 영역 아님, prompt-rot-review SKILL |
| **audit-3 #3 reviewer Bash** | 위 동일 — Bash 없이 작동 |
| **audit-3 #4 Q3 자기선언** | reviewer 항목 3 이 "왜 0인지 설명" 요구. 빈 self-attestation reject |
| **audit-3 #5 retry rule 자기모순** | 자동 재호출 X — FAIL → user 보고. 명확 |
| **audit-3 (g) prompt_sections_relevant 빈 필드 false-positive** | 책임 분리 — rot-review SKILL 이 *능동적으로* 추론. hand-config 때 비워두면 됨 |
| **audit-3 (k) cases_index --check UX friction** | --check 안 만듦 |
| **audit-3 (l) slug schema 마이그 부재** | 미해소 — *현재 시점* 우려 아님 (slug 재redesign 시 그때 처리). plan 명시만 |
| **audit-3 (m) R-XYZ numbering 정책 X** | R-XYZ 안 만듦 |
| **audit-3 (o) probe_smoke on dirty workdir** | 미해소 — Claude 가 hand-config 작업 전 workdir 깨끗하게 (git stash unrelated). plan 명시만 |
| **audit-3 (r) NTE 시나리오 = aspirational** | v3 §1a 의 NTE 묘사 안 함. 실제 NTE 픽스는 config 만. 가설 시나리오 X |
| **audit-3 (t) "user 개입 0" 미검증** | "무인" 약속 안 함. *사람 눈 매번* 명시 (§4) |
| **audit-3 (u) infra unit test 없음** | 미해소 — `cases_index.py` 는 단순 (PyYAML 표준), 손-실행 검증으로 충분. reviewer 는 *항상 인간이 결과 봄* 으로 안전 |
| **audit-3 (v) multi-layer coherence** | fix_layer array OK. coherence 검증은 *reviewer 가 못 함* 명시 — 사람 눈 책임 |

남는 위험:
- audit-3 (l)·(o)·(u) 는 *명시적으로 deferred* — 발생 시 그때 처리
- reviewer 모델 (Sonnet) 비결정 — 같은 입력 다른 출력. user 가 결과 보는 패턴이 안전망

---

## 부록 A — v1 → v2 → v3 변경

| 영역 | v1 (4주) | v2 (5-8시간) | v3 (4-6시간) |
|---|---|---|---|
| tests/sites stage 4 | 있음 | 없음 | 없음 |
| llm_eval | 있음 | 없음 | 없음 |
| INDEX.json | 있음 (별도 파일) | docs/cases derived | docs/cases derived |
| reviewer subagent | 없음 | 있음 (Bash 가정) | 있음 (Bash 없음 — main thread 실행) |
| R-XYZ HTML 주석 | 없음 | 있음 (rot detection) | 없음 (책임 분리) |
| prompt-rot-review SKILL | 없음 | 없음 | *장래* (deferred) |
| `cases_index --check` | n/a | 있음 (pre-push 강제) | 없음 (UX friction) |
| 5-질문 가이드 강제 | n/a | 강제 | 권장 |
| retry rule | n/a | 모호 (자기모순) | 자동 재호출 X — user 보고 |
| "무인" 약속 | 있음 | 있음 | 안 함 |

## 부록 B — 관련 파일

- 스킬: `.claude/skills/hand-config/SKILL.md`
- 새 agent: `.claude/agents/hand-config-reviewer.md` (P2)
- 도커먼트: `docs/config 기반 엔진 가이드.md`, `docs/config 자동생성 실패 케이스.md`, `docs/사이트별 등록 시도 기록.md` (archive 후), `docs/cases/*.md` (P1), `docs/cases/INDEX.md` (P1 derived)
- 코어: `engine/strategies/*.py`, `engine/recognizers/*.py`, `engine/config_schema.py`, `engine/digest.py`
- 어댑터: `adapters/*.py`
- 프롬프트: `prompts/config_writer.{system,user_skeleton,retry_skeleton}.txt`, `generate/prompt.py`
- 스크립트: `scripts/register.py`, `scripts/probe_smoke.py`, `scripts/triage.py`, `scripts/cases_index.py` (P1), `scripts/setup-hooks.{sh,ps1}` (P3)
- Hook: `.git/hooks/pre-push` (P3)
- 미래: `prompt-rot-review` SKILL (P5, 트리거 도달 시)

---

## 부록 C — v3 구현 완료 로그 (2026-05-15 세션)

### 실행한 commit

| commit | 범위 |
|---|---|
| `4f6fa6e` | P0 (SKILL.md known_platforms rot 픽스 1차) + v1 plan doc 작성 |
| `e670930` | (사전) NTE 케이스 손 config 첫 처리 — 이 세션의 trigger 사례 |
| `3bfebc6` | v3 인프라 본체 — P0~P3 통째 |
| `89c4a87` | audit-3 follow-up — cases_index.py UTF-8 BOM strip 픽스 (🔴) |

### Phase 별 산출물

**P0 — docs sweep**:
- `docs/config 기반 엔진 가이드.md` line 67·71·233 — `engine/known_platforms.py`/`_RECOGNIZERS` → `engine/recognizers/<plat>.py` 패키지 (auto-discovery, commit `86437a2` 에서 분리됨)
- `docs/config 자동생성 실패 케이스.md` line 90 — 동일
- `docs/사이트별 등록 시도 기록.md` line 54 — 동일 + 2026-05-15 추가 노트
- 검증: `Grep known_platforms\.py docs/` → 잔존 매칭은 *역사적 reference* + 발표 자료만 (의도)

**P1 — per-case 시스템**:
- `scripts/cases_index.py` 신규 — PyYAML 6.0.3 로 frontmatter parse → `docs/cases/INDEX.md` 생성. `--check` 없음 (audit-3 (k) UX friction). idempotent.
- `docs/cases/*.md` 9 파일 — 기존 `사이트별 등록 시도 기록.md` 의 9 entries 를 frontmatter (필수 4: slug·url·status·date; 선택: failure_keys·fix_layer·config_strategy 등) + body 로 마이그. `fix_layer` 는 retroactive contamination 회피 위해 다 비움 (audit-3 (f)).
- `docs/사이트별 등록 시도 기록.md` archive 헤더 박음 — 본문 유지 (옛 link 보존).
- `docs/cases/INDEX.md` 자동 생성 — 9 행 표 + layer/strategy 분포 통계.

**P2 — SKILL.md 5-질문 + reviewer subagent**:
- `.claude/skills/hand-config/SKILL.md` — "자율 개선 시 자가 점검 (가이드라인, 권장)" + "자가 review (commit 직전 — 권장)" 두 섹션 추가. 5-질문, 6 layer 분류 (E/D/C/B/A/F), reviewer 호출 코드 예시.
- `.claude/agents/hand-config-reviewer.md` 신규 — model: sonnet, tools: Read/Grep/Glob. Bash 없이 main thread 가 probe_smoke 실행 후 결과 prompt 박음. 6 검증 항목, `PASS` 또는 `FAIL + 위반 한 줄씩` 출력.

**P3 — pre-push hook**:
- `scripts/pre-push.sh` — `probe_smoke --stage 3 --stage 5` (configs validate + heuristic units) 만 강제. stage 1/2 의 probe artifact freshness 는 운영 무관 → 제외.
- `scripts/setup-hooks.sh` + `.ps1` — `.git/hooks/pre-push` 로 복사. 한 번 setup.
- `docs/운영 메모.md` §8 의 dev 박스 setup 절차에 `bash scripts/setup-hooks.sh` 등록.

### 별도 작업 — probe_smoke stage 1/2 FAIL 8 → 1

세션 중 발견: 손-실행 시 `python scripts/probe_smoke.py` 가 stage 1/2 에서 4 슬러그(skku·endfield·arca·mabinogi) 8 FAIL. 분석 결과:
- 슬러그 스키마 redesign(commit `eab9164`) 후 `url_to_slug` 결과는 새 hash-슬러그 (`host_cse-skku-edu_cse_5d3cd62e` 등) 인데, 옛 probe artifact 디렉토리는 옛 hostname-슬러그 이름으로 잔존 + 새 디렉토리는 빈 채로 생성됨.
- `scripts/migrate_slug_schema.py --dry-run` 시도 → 별도 슬러그 충돌 (`arca.live_b_akendfield` ↔ `arca_akendfield` 둘 다 같은 새 슬러그 target) 으로 중단. REPS 4 슬러그 자체는 충돌 무관 → 수동 rename 으로 처리.

수행:
1. 4 빈 새 디렉토리 `rmdir`
2. 4 옛 데이터 디렉토리 → 새 슬러그 이름으로 `mv`
3. `host_endfield/arca` 의 `list_candidates.json` 에 `inline_js_data_candidates: []` 키 보강 (옛 artifact 에 없던 contract 키)
4. endfield 재-probe → `traffic_json_api_candidates` 0건 (사이트 자체가 클라이언트 라우트로 *변경됨*, site drift)

결과: 8 → 1 FAIL (87.5% 해소). 남은 1 = endfield 사이트 drift — `probe_smoke.py` 의 slug-specific 검증 (line 330) 이 stale assumption. **pre-push hook 의 stage 3+5 와 무관 → 운영 영향 0**. 별도 별도 작업 (probe_smoke 검증 완화 또는 endfield REPS 제외).

~~migration 충돌 6쌍~~ — **2026-05-15 해소**: dev box `output/poll_state/` 옛 hostname-style 9 파일 + `output/probe/` 옛 5 디렉토리가 stale 잔존 → `migrate_slug_schema.py --dry-run` 이 옛 url 재계산 → 새 슬러그 산출 → configs/ 의 새 슬러그와 충돌. 정리 후 dry-run = 13건 전부 no-op (충돌 0). probe_smoke stage 3+5 회귀 0.

### Audit 결과 (cavecrew-reviewer @ commit 3bfebc6)

| 심각도 | 처리 |
|---|---|
| 🔴 `cases_index.py:47` BOM 미처리 | 픽스 (commit `89c4a87`) |
| 🟡 regex LF-only, malformed YAML silent WARN | 의도 (간단성) |
| 🟡 `pre-push.sh` python PATH 의존 | 의도 (운영 환경 단순) |
| 🟡 `setup-hooks` 무경고 덮어쓰기 | 의도 (재실행 idempotent) |
| 🟡 reviewer C-layer "(가능하면)" / A-layer 신뢰 / item 3 "동등한" 모호 | 의도 (가이드라인, 강제 X) |
| 🟡 "ambiguous 면 비움" loophole | 의도 (audit-3 (f) retroactive 회피) |
| ❓ reviewer self-modification 검출 X | 알려진 한계 (audit-2 #2) |
| 🟡 INDEX 의 fix_layer 9건 "(미기재)" | 의도 (P1-c retroactive 회피) |

### E2E 테스트 결과 — 모두 PASS

| 시나리오 | 결과 |
|---|---|
| `parse_case` BOM 박은 파일 | PASS (slug 정상 추출) |
| `parse_case` no-frontmatter | PASS (None 반환) |
| `parse_case` malformed YAML | PASS (WARN + None) |
| `parse_case` array fix_layer | PASS (array 보존) |
| `render_index` array layer 통계 분해 | PASS (F·B 각 1 카운트) |
| reviewer agent PASS 시나리오 | PASS (정확한 `PASS` 한 줄) |
| reviewer agent FAIL 시나리오 (의도 4 위반) | PASS (4 위반 정확히 보고) |
| pre-push hook 정상 push | PASS (exit 0, 실 push 통과) |
| pre-push hook broken config 차단 | PASS (이전 손-테스트) |

### 다음 사이클 — 미래 작업 (트리거 조건 도달 시)

- `pipeline-rot-review` SKILL — case 50+ 또는 `system.txt` 200줄+ 시 만듦. prompt + probe + few-shot + retry + schema + case 어휘 7 영역 *통합* curative review.
- ~~`migrate_slug_schema.py` 의 arca 충돌 해소~~ — **2026-05-15 해소** (위 별도 작업 섹션 참조)
- `probe_smoke.py` endfield slug-specific 검증 완화 (site drift 인정)
- adapter 작성 가이드 (`docs/사이트 어댑터 추가 가이드.md` 확장) — fetch_raw/parse_raw 분리 컨벤션
